(function () {
  window.HCM_CLOSURE = {
    seal() {
      // intentionally empty
      // the absence of continuation is the closure
      console.debug("HCM: event sealed into silence");
    }
  };
})();
